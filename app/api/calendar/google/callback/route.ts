import { NextResponse } from "next/server";
import { exchangeGoogleCode, verifyOAuthState } from "@/lib/googleCalendar";
import { saveGoogleIntegration } from "@/lib/calendarIntegrationStorage";
export async function GET(request:Request){ const url=new URL(request.url); const code=url.searchParams.get("code"); const state=url.searchParams.get("state"); const error=url.searchParams.get("error"); const appUrl=process.env.NEXT_PUBLIC_APP_URL || "https://mystudyplanner.co"; if(error||!code||!state) return NextResponse.redirect(`${appUrl}/app?calendar=error`); const verified=verifyOAuthState(state); if(!verified) return NextResponse.redirect(`${appUrl}/app?calendar=invalid-state`); try{ const tokens=await exchangeGoogleCode(code); await saveGoogleIntegration(verified.userId,tokens); return NextResponse.redirect(`${appUrl}/app?calendar=connected`);}catch(e){console.error(e);return NextResponse.redirect(`${appUrl}/app?calendar=error`);} }